.contents.row
  .container
    = form_with(model: @post, local: true) do |form|
      %h3 編集する
      = form.text_field :title, placeholder: "Title"
      = form.text_field :image, placeholder: "Image Url"
      = form.text_area :content, placeholder: "Content", rows: "10"
      = form.submit "SEND"
